{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "pnpm -r run build",
        "lint": "wx-lint --skip-knip",
        "publish": "pnpm build && pnpm publish --no-git-checks -r"
    },
    "devDependencies": {
        "@canale/starter": "workspace:*",
        "typescript": "^5.8.3",
        "vitest": "^3.1.1"
    }
}
