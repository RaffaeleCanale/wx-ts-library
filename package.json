{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "pnpm -r run build",
        "lint": "vp check",
        "publish": "pnpm build && pnpm publish -r --no-git-checks"
    },
    "devDependencies": {
        "@RaffaeleCanale/starter": "workspace:*",
        "@types/node": "^22.20.2",
        "typescript": "^7.0.2",
        "vite-plus": "^0.3.1",
        "vitest": "^5.0.0"
    }
}
